{
  "name": "harbord",
  "version": "0.1.2",
  "description": "Declarative Local Runtime Supervisor for Node.js Applications",
  "type": "module",
  "module": "dist/esm/index.js",
  "main": "dist/cjs/index.cjs",
  "types": "dist/types/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/esm/index.js",
      "require": "./dist/cjs/index.cjs"
    }
  },
  "bin": {
    "harbord": "dist/cjs/cli.cjs"
  },
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lszio/harbord.git"
  },
  "keywords": [
    "supervisor",
    "daemon",
    "runtime",
    "process-manager",
    "node",
    "reconciler"
  ],
  "author": "lszio",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/lszio/harbord/issues"
  },
  "homepage": "https://github.com/lszio/harbord#readme",
  "private": false,
  "scripts": {
    "dev": "bun run src/cli.ts --daemon",
    "build": "bun run build.ts",
    "build:all": "bun run build.ts",
    "clean": "rm -rf dist/ coverage/",
    "prepublishOnly": "bun run build",
    "version": "conventional-changelog -p angular -i CHANGELOG.md -s && git add CHANGELOG.md",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run",
    "test:e2e": "vitest run --config vitest.e2e.config.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "conventional-changelog-cli": "^5.0.0",
    "vitest": "^4.1.5"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "engines": {
    "node": ">=18",
    "bun": ">=1.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
